using System;
using PingPongGame.Models;
namespace PingPongGame
{
    class Program
    {
        static void Main()
        {
            // PingPongGame.Models.PingPong newNum = new PingPongGame.Models.PingPong();
            // Console.WriteLine(newNum.GetType());
            // int num = new int();
            // Console.WriteLine(num.GetType());
        }
    }
}